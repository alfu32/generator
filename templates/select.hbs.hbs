SELECT
{{#every fields}}
{{../alias}}.{{name}} {{alias}} {{#if !@last}},{{/if}}
{{/every}}
FROM {{name}} {{alias}}
WHERE
{{#hbs '<#every filter><../alias>.<@key>=\'this\'<#if !@last> AND </if><#every>'}}{{#hbs}}

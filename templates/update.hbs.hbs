UPDATE {{name}} {{alias}}
SET {{#every field}}
{{../alias}}.{{name}}='item.\{\{{{name}}\}\}'{{#if !@last}},{{/if}}
{{/every}}
WHERE 
{{#hbs '<#every filter>'}}{{/hbs}}{{alias}}.{{#hbs '<@key>=\'<this>\'<#if !@last> AND </if></every>' delims='<>'}}{{/hbs}}

